#!/usr/bin/env python

from distutils.core import setup

setup(name='pypixoto',
      version='1.0',
      description='Python SDK for accessing the data from pixoto.com',
      author='Daxeel Soni',
      author_email='sayhi@daxeelsoni.in',
      url='http://www.daxeelsoni.in',
      keywords = ['pypixoto', 'pixoto', 'photography', 'photo', 'photographer'],
      py_modules=['pypixoto'],
     )
